/*
猜字游戏：
先输入2个不超过100的正整数，分别是被猜数mynumber和允许猜测的最大次数n，
再输入你所猜的数yournumber，与被猜数进行比较，
若相等，显示猜中；若不相等，显示与被猜数的大小关系，最多允许猜n次。
如果1次就猜中该数，提示"Bingo!"；如果3次内猜到该数，提示"Lucky You!"，
如果超过n次都没猜到，则提示"Game Over"；如果到达n次之前，用户输入一个负数，
也输出"Game Over"，并结束程序。
*/